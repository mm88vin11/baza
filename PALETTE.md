# Палитра сайта БАЗА

Тёплая палитра, снятая с референсного видео. Пять цветов задают весь сайт,
подложка и «бумага» — производные от них.

| Роль | HEX | Где живёт |
|---|---|---|
| Амбер | `#d37724` | бегущая строка, выделенная карточка, тёплое свечение фона |
| Золото | `#eea252` | таблетка активного пункта меню, выбранные чипы, точки шагов и услуг, заливка кнопок при наведении |
| Вино | `#831a0a` | фавикон, нижнее свечение фона, кант главной кнопки брифа |
| Коралл | `#f05035` | главный CTA, кнопка брифа, ползунок вилки бюджета, сердечки, выделение текста, падение в тикере |
| Бумага | `#f7efe4` | текст на тёмном, светлые панели (бриф, тариф «вам сюда»), логотип |
| Подложка | `#1a0805` | фон страницы и тёмные поверхности |

Тикер показателей: рост — золото `#eea252`, падение — коралл `#f05035`
(направление дополнительно читается стрелкой ▲/▼).

## Как собрано

Исходник — однофайловый экспорт сайта: разметка, стили и все ассеты
(секвенции кадров, логотипы) лежат в одном HTML. Перекраска сделана
скриптом `tools/recolor.py` в три слоя:

1. **Токены.** В исходнике вся тема держалась на двух литералах —
   `#0a0a0a` и `#f2ebdd` (плюс их rgba-производные). Они заменены на
   `#1a0805` и `#f7efe4`, тени и промежуточные серые переведены в тёплые.
   Флаги стран в блоке гео не трогали — у них свои цвета.
2. **Акценты.** Точечные правки в местах, где раньше стояла сплошная
   кремовая заливка: главный CTA и кнопка брифа получили градиент
   коралл→амбер, активная таблетка навигации и чипы — золото, бегущая
   строка — градиент амбер→коралл, выделенная карточка выгоды — амбер.
3. **Атмосфера.** В фоновый слой добавлено тёплое свечение (коралл сверху
   слева, золото справа, вино снизу), а все 269 кадров секвенций прогнаны
   через gradient map по яркости: тени садятся ровно в цвет подложки,
   середина уходит в амбер, света — в бумагу.

Запуск:

```
python3 tools/recolor.py <исходный экспорт>.html index.html
```
